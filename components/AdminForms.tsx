export function Text({ name, label, defaultValue = "", type = "text", required = false }: { name: string; label: string; defaultValue?: string | number | null; type?: string; required?: boolean }) { return <label className="admin-label">{label}<input className="admin-input" name={name} type={type} defaultValue={defaultValue ?? ""} required={required}/></label>; }
export function Area({ name, label, defaultValue = "", required = false }: { name: string; label: string; defaultValue?: string | null; required?: boolean }) { return <label className="admin-label">{label}<textarea className="admin-input min-h-28" name={name} defaultValue={defaultValue ?? ""} required={required}/></label>; }
export function Check({ name, label, defaultChecked = false }: { name: string; label: string; defaultChecked?: boolean }) { return <label className="flex items-center gap-2 text-sm font-semibold text-midnight"><input type="checkbox" name={name} defaultChecked={defaultChecked}/>{label}</label>; }
export function SeoPreview({ title, description, url }: { title?: string | null; description?: string | null; url: string }) { return <div className="rounded-2xl border bg-white p-4"><p className="text-xs text-slate-500">Google preview</p><p className="mt-2 text-lg text-blue-700">{title || "SEO title appears here"}</p><p className="text-sm text-green-700">{url}</p><p className="text-sm text-slate-600">{description || "SEO description appears here."}</p></div>; }
